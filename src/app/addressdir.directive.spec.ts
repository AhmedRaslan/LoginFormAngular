import { AddressdirDirective } from './addressdir.directive';

describe('AddressdirDirective', () => {
  it('should create an instance', () => {
    const directive = new AddressdirDirective();
    expect(directive).toBeTruthy();
  });
});
